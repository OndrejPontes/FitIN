<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="150dp"
    android:focusable="true"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp"
    android:clickable="true"
    android:orientation="vertical"
    android:id="@+id/my_filters_item_container">

    <TextView
        android:id="@+id/my_filters_item_name"
        android:textColor="@color/title"
        android:text="Example name"
        android:textSize="16dp"
        android:textStyle="bold"
        android:layout_alignParentTop="true"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/my_filters_item_equipments"
        android:layout_below="@id/my_filters_item_name"
        android:text="Example equipment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/my_filters_item_activities"
        android:layout_width="wrap_content"
        android:layout_below="@id/my_filters_item_equipments"
        android:text="Example activities"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/my_filters_item_count"
        android:text="15"
        android:layout_width="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_height="wrap_content" />

</RelativeLayout>